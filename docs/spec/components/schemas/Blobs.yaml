allOf:
  - $ref: '#/components/schemas/BlobKey'
  - type: object
    required: [ attributes ]
    properties:
      attributes:
        type: object
        required: [
            owner,
            content,
        ]
        properties:
          owner:
            type: string
            description: blob owner
          content:
            type: string
            description: custom json
